# config/__init__.py
#
# The project configuration package. Deliberately empty - nothing should import from
# here, because a package that both configures the project and exports code becomes a
# circular-import problem the moment settings grow.
