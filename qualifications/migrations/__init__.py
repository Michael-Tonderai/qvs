# qualifications/migrations/__init__.py
#
# Empty, but present. Without this package Django will not record migrations for the
# app, and the first `makemigrations` in Sprint B would silently write nothing.
