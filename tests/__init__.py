# tests/__init__.py
#
# Tests live in one root package rather than beside the code, and the unit /
# integration split is carried by pytest markers rather than by directories (D-004).
# One mechanism, so the traceability matrix and the assessed unit-versus-integration
# split are generated from the same source of truth.
